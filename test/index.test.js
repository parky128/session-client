/* eslint-disable */
const should = require('chai').should();

const test = 1;

describe('#stuff', function() {
  it('passes testing', function() {
    test.should.equal(1);
  });
});
/* eslint-enable */
